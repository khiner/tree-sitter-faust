=====
Recursion
=====

process = _~+(1);

---
(program
  (definition (process)
    (recursive
      (wire)
      (partial (add) (int)))))

=====
Sequential
=====

process = _:_;

---
(program
  (definition (process)
    (sequential (wire) (wire))))

=====
Split two
=====

process = _<:_,_;

---
(program
  (definition (process)
    (split
      (wire)
      (parallel (wire) (wire)))))

=====
Split three
=====

process = _ <: _,_,_;

---
(program
  (definition (process)
    (split
      (wire)
      (parallel
        (wire)
        (parallel (wire) (wire))))))

=====
Merge two
=====

process = _,_ :> _;

---
(program
  (definition (process)
    (merge
      (parallel (wire) (wire))
      (wire))))

=====
Merge three
=====

process = _,_,_ :> _;

---
(program
  (definition (process)
    (merge
      (parallel
        (wire)
        (parallel (wire) (wire)))
      (wire))))
