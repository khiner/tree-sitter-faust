====
Int
====

process = 1,+10,-100;

---
(program
  (definition (id)
    (parallel
      (int)
      (parallel (int) (int)))))

====
Real
====

process = 0.5,.5,5.,+1.,-1.f,500.5e4,-500e40f;

---
(program
  (definition (id)
    (parallel (real)
      (parallel (real)
        (parallel (real)
          (parallel (real)
            (parallel (real)
              (parallel (real) (real)))))))))
